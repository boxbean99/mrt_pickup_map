# Turkey Planner — 세션 Handover

**작성일**: 2026-04-28  
**다음 세션 시작 위치**: Phase 2 — Vercel 배포 & 실제 MRT API 연동 준비

---

## 프로젝트 기본 정보

- **로컬 작업 경로**: `C:\Users\User\turkey-planner`
- **배포 대상**: Vercel (미배포 상태, 다음 세션에서 진행)
- **임베드 방식**: `<iframe src="https://<vercel-url>">` 한 줄 삽입
- **GitHub**: 미연결 (로컬 git만 초기화된 상태)

---

## 기술 스택

| 항목 | 버전/선택 |
|------|-----------|
| Next.js | 14.2.35 (App Router) |
| TypeScript | 5.x |
| Tailwind CSS | 3.x |
| 지도 | react-simple-maps + world-atlas TopoJSON (타일 없음, 외부 요청 0) |
| 상태관리 | React useState (라이브러리 없음) |
| 데이터 | mock-products.json (정적 JSON, 17개 상품) |

---

## 완료된 작업 (Phase 1 — 프로토타입)

- [x] Next.js 14 프로젝트 생성 + Tailwind 설정
- [x] react-simple-maps + world-50m.json 설치 (Leaflet 타일 대체)
- [x] 타입 정의 (`types/index.ts` — City, PlannerState, Product, TravelStyle)
- [x] 도시 데이터 (`data/cities.ts` — 5개 도시 좌표/메타)
- [x] mock 상품 데이터 (`data/mock-products.json` — 도시별 픽업·투어·액티비티 17개)
- [x] MapView 컴포넌트 — 터키 지도 렌더링, 도시 핀, 경로 라인
- [x] **확대/축소 기능** — ZoomableGroup (+/− 버튼, 드래그 이동, ⊙ 리셋, 휠 스크롤)
- [x] 4단계 위저드: Step1(도시) → Step2(날짜) → Step3(스타일) → Step4(상품)
- [x] 픽업/샌딩 섹션 Step4 최상단 고정
- [x] API route (`/api/products`)
- [x] `npm run build` 성공, 타입 오류 없음

---

## 파일 구조

```
turkey-planner/
├── app/
│   ├── page.tsx                  # 메인 페이지 (PlannerWidget 마운트)
│   ├── layout.tsx                # 메타데이터 설정
│   └── api/products/route.ts     # mock 상품 API
├── components/
│   ├── PlannerWidget.tsx         # 전체 상태 관리 (useState)
│   ├── MapView.tsx               # react-simple-maps 지도 + 줌
│   ├── StepPanel.tsx             # 우측 패널 + 스텝 인디케이터
│   └── steps/
│       ├── Step1Cities.tsx       # 도시 선택 UI
│       ├── Step2Dates.tsx        # 숙박일 설정 + AI 추천
│       ├── Step3Style.tsx        # 여행 스타일 4종 선택
│       └── Step4Products.tsx     # 상품 카드 + 픽업 고정
├── data/
│   ├── cities.ts                 # CITIES 배열, CITY_MAP, DEFAULT_NIGHTS
│   └── mock-products.json        # 17개 mock 상품 (도시별 픽업 포함)
├── types/index.ts                # 공용 타입 정의
├── public/
│   └── world-50m.json            # Natural Earth 50m TopoJSON (756KB)
└── docs/
    ├── handover-2026-04-28.md    # 이 파일
    └── superpowers/specs/
        └── 2026-04-28-turkey-planner-design.md
```

---

## 다음 세션 작업 내용

### 우선순위 1: Vercel 배포
```bash
# Vercel CLI 설치 (없는 경우)
npm i -g vercel

# 프로젝트 루트에서
vercel

# 배포 완료 후 iframe 코드 확인
# <iframe src="https://<project>.vercel.app" width="100%" height="600" frameborder="0"></iframe>
```

### 우선순위 2: .gitignore 보완
`.superpowers/` 항목 추가 필요:
```
.superpowers/
```

### 우선순위 3: 실제 MRT API 연동 준비 (2차)
- `/app/api/products/route.ts` → MRT 상품 API proxy로 교체
- 환경변수 추가: `MRT_API_BASE_URL`, `MRT_API_KEY`
- 도시 ID ↔ MRT 카테고리 코드 매핑 테이블 작성 필요

### 우선순위 4: UX 개선 (선택)
- Step4 결과에서 "마이리얼트립 앱으로 보기" 딥링크 추가
- 모바일 반응형 레이아웃 (현재 고정 265px 패널)
- 선택 도시 순서 드래그로 변경 기능

---

## 알려진 이슈 / 주의사항

| 항목 | 내용 |
|------|------|
| GitHub 미연결 | `git remote`가 없음. 배포 전 GitHub repo 생성 후 연결 필요 |
| 상품 이미지 | Unsplash URL 하드코딩 — 실제 MRT 썸네일로 교체 필요 |
| 상품 URL | `myrealtrip.com/offers/...` 하드코딩 — 실제 상품 ID 기반으로 변경 필요 |
| dev 서버 재시작 | 세션 사이에 `npm run dev`로 재시작 필요 (`localhost:3000`) |
| `.superpowers/` | `.gitignore`에 미포함 — 커밋 전 추가 필요 |

---

## 운영 규칙

| 항목 | 규칙 |
|------|------|
| git push | 사용자가 직접 실행 |
| Vercel 배포 | 사용자가 직접 실행 (`vercel` CLI) |
| 빌드 실패 | 해결 전 커밋 금지 |
| MRT API 키 | `.env.local`에 보관, 코드에 직접 노출 금지 |
