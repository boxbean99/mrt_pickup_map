# 터키 여행 플래너 — 세션 Handover
**작성일**: 2026-05-12  
**다음 세션 시작 위치**: Phase 2 — GitHub 연결 & Vercel 배포 & MRT API 연동

---

## 프로젝트 기본 정보
- **로컬 작업 경로**: `C:\Users\User\turkey-planner`
- **GitHub**: 미연결 (push 필요)
- **Vercel**: 미배포
- **외부 서비스**: 없음 (현재 mock 데이터 사용)

---

## 기술 스택
- Next.js 14.2.35 + TypeScript + Tailwind CSS
- 지도: react-simple-maps + world-50m TopoJSON (타일 없음, 순수 SVG)
- 상태: React useState
- 데이터: mock (JSON + TypeScript 상수)

---

## 완료된 작업

### Phase 1 — 프로토타입 (이전 세션)
- [x] Next.js 프로젝트 초기화
- [x] 터키 지도 렌더링 (react-simple-maps)
- [x] 도시 마커 + 선택/순서 표시 + 경로선
- [x] 4단계 StepPanel (도시 선택 → 박수 설정 → 여행 스타일 → 상품 추천)
- [x] 줌 컨트롤 (확대/축소/초기화)
- [x] mock 상품 데이터 연결

### Phase 1.5 — 장소 타입 확장 (2026-05-12)
- [x] `LocationType` 타입 추가 (`city` | `airport` | `attraction` | `nearby`)
- [x] `City` 타입에 `type`, `parentCityId` 필드 추가
- [x] 공항 6개 추가 (IST, SAW, AYT, ASR, ADB, ESB)
- [x] 관광지 6개 추가 (보드룸, 마르마리스, 알라냐, 콘야, 트라브존, 차낙칼레)
- [x] 근교 도시 6개 추가 (부르사, 쿠사다스, 괴레메, 위르귑, 셀추크, 데니즐리)
- [x] 지도 마커: 타입별 색상/크기 구분 (빨강·파랑·주황·초록), z-order 정렬
- [x] 범례 업데이트 (4가지 타입)
- [x] Step1Cities 패널: 타입별 섹션 그룹화 + 타입 뱃지 표시

---

## 파일 구조

```
turkey-planner/
├── app/
│   ├── page.tsx              # 루트 페이지 (PlannerWidget 마운트)
│   ├── layout.tsx            # 공통 레이아웃
│   └── api/                  # API 라우트 (현재 미사용)
├── components/
│   ├── PlannerWidget.tsx     # 최상위 위젯 컨테이너, 상태 관리
│   ├── MapView.tsx           # 터키 지도, 타입별 마커 렌더링
│   ├── StepPanel.tsx         # 4단계 패널 라우터
│   └── steps/
│       ├── Step1Cities.tsx   # 장소 선택 (타입별 그룹)
│       ├── Step2Dates.tsx    # 박수 설정
│       ├── Step3Style.tsx    # 여행 스타일 선택
│       └── Step4Products.tsx # 상품 추천 목록
├── data/
│   ├── cities.ts             # 전체 장소 데이터 (5+6+6+6 = 23개)
│   └── mock-products.json    # mock 상품 데이터
├── types/
│   └── index.ts              # LocationType, City, Product 등 타입 정의
└── docs/
    ├── handover-2026-04-28.md
    └── handover-2026-05-12.md (현재 파일)
```

---

## 다음 세션 작업 내용 (Phase 2)

### 우선순위 순
1. **`.gitignore`에 `.superpowers/` 추가** 후 GitHub repo 연결 & 첫 push
2. **Vercel 배포** — `vercel` CLI로 배포 (사용자가 터미널에서 직접 실행)
3. **MRT 실제 상품 API 연동**
   - `/app/api/products/route.ts` 생성
   - MRT 상품 검색 API 엔드포인트 확인 필요
   - `cityId` 기반 필터링
4. **공항 마커 인터랙션 개선** (선택 시 박수 설정 불필요하도록 처리 고려)

---

## 알려진 이슈 / 주의사항

- **미커밋 상태**: 초기 커밋 이후 모든 실질적 작업이 untracked/unstaged. GitHub 연결 전 커밋 필요
- **공항의 박수 설정**: 공항은 숙박 개념이 없으나 현재 Step2에서 박수 설정 UI가 그대로 뜸. Phase 2에서 `type === 'airport'`는 박수 설정 스킵하거나 "경유" 표시로 분기 처리 필요
- **mock 상품**: `data/mock-products.json`의 `cityId`가 기존 5개 도시만 커버. 신규 장소 추가 시 상품 데이터도 확장 필요
- **괴레메/위르귑 좌표**: 카파도키아 중심과 매우 근접해 줌인 시에만 구분 가능

---

## 운영 규칙

- `git push` 및 Vercel 배포는 **사용자가 터미널에서 직접 실행** (Claude 직접 실행 금지)
- 빌드 실패 상태에서 커밋 금지
